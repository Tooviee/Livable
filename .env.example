# Supabase (create a project at https://supabase.com)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
# Required for admin (list/update requests). Server-only; never expose to client.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# Confirmation emails — use Brevo if Outlook SMTP is disabled for your account
# Brevo (free tier): brevo.com → API Keys, add a Sender (your email) and verify it
BREVO_API_KEY=xkeysib-xxxx
FROM_EMAIL=yourname@outlook.com
FROM_NAME=Livable
REPLY_TO_EMAIL=yourname@outlook.com

# Admin: secret to view /admin and call /api/requests
ADMIN_SECRET=your-random-secret

# Public URL for links in emails (reschedule, Zoom join, etc.). Set to your deployed URL so links work on phones and other devices (e.g. https://your-app.vercel.app). If unset, links use the request host (localhost in dev).
# APP_URL=https://your-actual-domain.com

# Optional: Discord webhook — get notified in a channel when a new request is submitted
# Server Settings → Integrations → Webhooks → New Webhook, copy the URL
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Optional: Auto-create Zoom meetings (Server-to-Server OAuth app at marketplace.zoom.us)
# Create a S2S OAuth app, add scope "Create a meeting for a user" (meeting:write:meeting:admin), then copy credentials.
# ZOOM_ACCOUNT_ID=your-account-id
# ZOOM_CLIENT_ID=your-client-id
# ZOOM_CLIENT_SECRET=your-client-secret
# ZOOM_USER_ID=me
# APP_TIMEZONE=Asia/Seoul

# Optional: When a request is deleted from Supabase (e.g. in Table Editor), call this app to cancel the Zoom meeting.
# Set SUPABASE_WEBHOOK_SECRET and add a Database Webhook in Supabase (DELETE on public.requests) to: https://your-app.com/api/webhooks/supabase-request-deleted with header x-webhook-secret = your secret.
# SUPABASE_WEBHOOK_SECRET=your-webhook-secret
